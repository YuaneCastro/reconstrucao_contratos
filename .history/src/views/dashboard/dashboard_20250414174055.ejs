<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Painel do Encarregado</title>
  <link rel="stylesheet" href="/assets/css/dashboard.css">
</head>
<body>
  <div class="container">
    <!-- SIDEBAR -->
    <nav class="sidebar">
      <div>
        <h2>🎓 Escola</h2>
        <ul>
          <li onclick="showSection('estudantes')">👨‍👩‍👧 Estudantes</li>
          <li onclick="showSection('contratos')">📜 Contratos</li>
          <li onclick="showSection('log')">📘 Atividades</li>
          <li onclick="showSection('normas')">📚 Normas</li>
          <li onclick="showSection('ajuda')">❓ Ajuda</li>
        </ul>
      </div>

      <div class="footer">
        <p><strong>Contato:</strong></p>
        <p>📞 (99) 99999-9999</p>
        <p>📧 contato@escola.com</p>
        <p>📍 Rua das Flores, 123 – Centro</p>
      </div>
    </nav>

    <!-- CONTEÚDO PRINCIPAL -->
    <main class="main">
      <!-- TOPBAR -->
      <div class="topbar">
        <div class="greeting">Olá, <strong><%= nome %></strong>!</div>
        <div class="actions">
          <button>Notificações</button>
          <button>Sair</button>
        </div>
      </div>

      <!-- CONTEÚDO DINÂMICO -->
      <div class="content">
        <div id="estudantes" class="section active">
          <h3>Estudantes Representados</h3>
          <ul>
            <li>Maria Eduarda - 9º ano A</li>
            <li>João Pedro - 5º ano B</li>
          </ul>
        </div>

        <div id="contratos" class="section">
          <h3>Contratos</h3>
          <p>Aqui você poderá visualizar e assinar eletronicamente os contratos escolares.</p>
        </div>

        <div id="log" class="section">
          <h3>Registro de Atividades</h3>
          <table>
            <thead>
                <tr>
                    <th>Açao</th>
                    <th>Data</th>
                </tr>
            </thead>
            <tbody>
                <% if(Atividades.length > 0){ %>
                    <% Atividades.forEach(a =>{  %>
                        <tr>
                            <td><%= students.id %></td>
                            <td><%= students.nome %></td>
                            <td><%= new Date(students.data_nascimento).toISOString().split('T')[0] %></td>
                            <td><%= students.classe %></td>
                            <td><%= students.turma %></td>
                            <td><%= students.curso%></td>
                            <td><%= students.encarregado_id %></td>
                            <td> 
                                <span onclick="showSection('edit_studentes')"
                                class="edit-btn-student"
                                data-id="<%= students.id %>"
                                data-nome="<%= students.nome %>"
                                data-classe="<%= students.classe %>"
                                data-turma="<%= students.turma %>"
                                data-curso="<%= students.curso%>">
                                ✏️
                                </span>
                            </td>
                        </tr>
                    <% });  %>   
                <% }else{ %>
                    <tr>
                        <td colspan="8">Nenhum estudante encontrado.</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
        </div>

        <div id="normas" class="section">
          <h3>Normas da Escola</h3>
          <p>Consulte o regulamento interno <a href="#">clicando aqui</a>.</p>
        </div>

        <div id="ajuda" class="section">
          <h3>Ajuda</h3>
          <p>Este sistema permite acompanhar o desempenho dos alunos, assinar contratos escolares, receber comunicados, visualizar normas e manter contato com a escola.</p>
        </div>
      </div>
    </main>
  </div>
  
  <script src="/assets/js/dashboard.js"></script>
</body>
</html>
